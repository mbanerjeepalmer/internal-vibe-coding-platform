
- Overall: We should have a browser window within the browser that, at the full viewport size, shows the current 'URL' (i.e. this is almost correct for maurices-non-technical-friends.vibe.kitchen/login and  mail.example.com/inbox).
- Wording should be 'Join 'Maurice's non-technical friends'. Should say that the link will expire in 15 minutes.
  - Login step should not require a password. 'Join Maurice's kitchen' and just confirm name and profile photo.
- The chat interface should be a faithful representation of something like Claude Code, Codex or OpenCode on the desktop or mobile. That means we need the text input field, voice input, ability to add attachments (you'll probably want to effectively clone OpenCode's web interface https://github.com/anomalyco/opencode).
  - Likewise the preview pane behaviour (I assume it only appears in reality once the build has started, I don't know what the standard behaviour is).
  - The attachment of the video with audio should work -- if OpenCode doesn't support this then ask for clarification.
- After submitting the voice note video, we should see that the agent working through it. (We can tighten this later, I'll do a recording of Codex doing this for real.)
- We should have all the appropriate level of latency, loading animations, interaction affordances like pre-suggested text and free input.
  - Again, if you don't have enough information about how OpenCode/Claude Code/Codex do this, you can ask me for screenshots/videos.
- Request for Google Maps API key should also give instructions for how to get one. The input field for the key should require a value -- just because we're hardcoding doesn't mean we can't implement validation.
- Afterwards we should see a vaguely-believable series of agent steps of writing tests and code, and running tests.
- The 'Deploy to Cloudflare' should likewise be believable -- think about how it would look in one of the coding agents' web interfaces. Then the user can visit the URL and see the error.
  - Note: The deploy should be to the staging env -- not sure what the button wording should be.
- We don't need a big button that says 'Back to the Vibe Coding Platform'. It should be a back button in the browser frame.
- When we go back, we should be back in the conversation. The way that Sentry and PostHog integrate is with their respective MCPs. So the user in the conversation can say 'The application crashed. Check PostHog and Sentry.'. Then the agent follows a believable path to fix it, including using local Wrangler as the dev server instead of Vite.
  - Note: Fact check me on this Wrangler/Vite point.
- Finally, the agent suggests 'This could have been avoided if I had used a different development server. Should I propose a change to the Kitchen configuration?'. We accept the change.
- We should be able to go to the 'Kitchen', which is called 'Maurice's non-technical friends'. We can see our own project. We should be able to see the existing Daytona config and the proposed change. We can also create our own Kitchen.
  - Note: In the home directory that this worktree branches from, we're still working out exactly how this config system works, so don't worry too much about the details.
- We log out -- this doesn't need to be a single click, it is genuinely clicking the account menu then logging out.
- Then we get a magic link to banquetapptesting@gmail.com and find ourselves logged in as Maurice.
- Now we can see the dashboard including the notification that Alexandra has proposed a change to the config in 'Maurice's non-technical friends'. (We also see a few other Kitchens like 'Claudia - Team', 'MBP test' and 'Finance apps'.)
- We approve her change and re-run the deploy pipeline.
